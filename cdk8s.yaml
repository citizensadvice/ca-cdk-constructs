language: python
imports:
  - k8s@1.36.0
  # must match the version deployed to the cluster
  # find the currently deployed version by inspecting the controller pod images:
  #     kubectl get --template='{{(index .spec.template.spec.containers 0).image}}' deploy -n kube-system external-secrets"
  #
  # original at https://raw.githubusercontent.com/external-secrets/external-secrets/v2.8.0/deploy/crds/bundle.yaml
  # modified to comment/disable fields like "getXxx" as they make the import fail
  # see https://github.com/cdk8s-team/cdk8s/issues/1323
  - ./crds/external-secrets-crds.yaml
